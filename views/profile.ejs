<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full p-10 bg-zinc-900 text-white ">
       
        <div class="w-full flex justify-between mb-5">
            <a href="/profile/upload" class="text-white px-3 py-2 bg-red-500 border-2 border-zinc-800 rounded-lg inline-block">Upload a profile pic</a>

            <a href="/logout"
                class="text-white px-3 py-2 bg-red-500 border-2 border-zinc-800 rounded-lg inline-block">Logout</a>
        </div>
        
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 rounded-md ">
                <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic %>">
            </div>
            <h1 class="text-3xl tracking-tighter mb-5">Welcome <%= user.name %> 🤯</h1>
        </div>
        <h5 class="mb-5">You can create a new post</h5>
        <!-- //create a new post -->
        <form action="/post" method="post">
            <textarea class=" w-1/3 px-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                placeholder="what's in your mind" name="content"></textarea>
            <input type="submit" class="px-3 w-40 py-2 text-sm  bg-blue-500 block ">
        </form>


        <div class="posts mt-20">
            <div class="w-full max-w-lg p-4 ">
                <h1 class="text-xl font-bold mb-4">Your Posts</h1>

                <!-- Post 1 -->
                <%user.posts.reverse().forEach(function(post){ %>

                    <div class="bg-gray-800 p-4 rounded-lg shadow mb-4">
                        <p class="text-blue-400 font-semibold">@<%=user.username%>
                        </p>
                        <p class="text-gray-300 mt-2">
                            <%= post.content %>
                        </p>
                        <small class="text-sm mb-4">
                            <%=post.likes.length%>likes
                        </small>
                        <div class="btns flex items-center space-x-4 mt-4">
                            <a class="text-blue-400" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            
                            <a class="text-blue-400" href="/edit/<%=post._id %>">Edit</a>
                            <a href="/delete/<%=post._id %>">Delete</a>
                        </div>
                    </div>

                    <% }) %>


            </div>

        </div>

    </div>
</body>

</html>